export function compressGameData(data) {
    // take a game data object, compress it, return a string for use in a uri

    // convert to string
    // remove all ""
}

export function decompressGameData(str) {
    // take a compressed game data string (used in uri) and return it as game data object
}